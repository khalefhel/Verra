<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VERRA" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.VERRA" parent="Base.Theme.VERRA" />

    <!-- Créer le composant titre par defaut Inscription & Connexion -->
    <style name="DefaultTextStyle">
        <item name="android:textColor">@color/brown</item>
        <item name="android:textSize">13dp</item>
        <item name="android:fontFamily">@font/cinzel</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ButtonTextStyle" parent="@style/DefaultTextStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">20dp</item>
    </style>

    <style name="TitleTextStyle" parent="@style/DefaultTextStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">40dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:tint">@color/darkGray</item>

        <item name="android:shadowColor">@color/black</item>
        <item name="android:shadowRadius">5</item>
    </style>

    <!-- Payment page-->
    <style name="s_payment_title">
        <item name="android:textSize">20dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
        <item name="fontFamily">@font/cinzel</item>
    </style>

    <!-- Input -->
    <style name="input">
        <item name="fontFamily">@font/cinzel</item>
    </style>

    <!-- Home page -->
    <style name="AdvertisementText">
        <item name="android:textSize">15dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="AdvertisementTextDescription">
        <item name="android:textSize">10dp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="HomeCommandTextStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">13dp</item>
    </style>

    <style name="HomeFilterTextStyle" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/brown</item>
        <item name="android:textSize">12dp</item>
    </style>

    <style name="ArticleDescriptionStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">10sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ArticleNameStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">10sp</item>
    </style>

    <!-- Creer un style pour la bottom navigation view -->
    <style name="BottomNavigationStyle" parent="Widget.Design.BottomNavigationView">
        <item name="android:textSize">18sp</item>
        <item name="fontFamily">@font/cinzel</item>
    </style>

    <!-- personnaliser la barre de navigation-->
    <style name="BottomNavigationTheme" parent="Widget.Design.BottomNavigationView">
        <item name="android:textSize">18sp</item>
        <item name="fontFamily">@font/cinzel</item>
    </style>

</resources>